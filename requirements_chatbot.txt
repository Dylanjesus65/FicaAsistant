# Requirements para ejecutar el Chatbot (venv312)
# Documentado: 2026-01-20
# Python 3.12 requerido

# Web Framework
django>=4.2
channels>=4.0
daphne>=4.0

# ML/Inference
torch>=2.1.0
# Instalar con: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
transformers>=4.40.0
peft>=0.7.0
bitsandbytes>=0.41.0
accelerate>=0.25.0

# Utils
pyyaml
scipy

# NOTAS:
# 1. Requiere GPU NVIDIA con CUDA 12.1+
# 2. Mínimo 8GB VRAM
# 3. Instalar PyTorch con soporte CUDA primero
